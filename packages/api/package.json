{
  "name": "@trail/api",
  "version": "0.1.0",
  "description": "Cloudflare Worker — receives touchpoints, stores in D1, serves attribution data",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "generate:tracker": "node -e \"const fs=require('fs');const c=fs.readFileSync('../tracker/dist/t.js','utf8');fs.writeFileSync('src/tracker.ts','export const TRACKER_SCRIPT = '+JSON.stringify(c)+';\\n');\"",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.57.0"
  }
}
